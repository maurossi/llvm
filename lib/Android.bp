cc_defaults {
    name: "llvm60-lib-defaults",
    vendor_available: true,
    defaults: ["llvm60-defaults"],
    export_header_lib_headers: ["llvm60-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
