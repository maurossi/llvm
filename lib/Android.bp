cc_defaults {
    name: "llvm50-lib-defaults",
    vendor_available: true,
    defaults: ["llvm50-defaults"],
    export_header_lib_headers: ["llvm50-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
