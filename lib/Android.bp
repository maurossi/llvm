cc_defaults {
    name: "llvm80-lib-defaults",
    vendor_available: true,
    defaults: ["llvm80-defaults"],
    export_header_lib_headers: ["llvm80-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
