cc_defaults {
    name: "llvm10-lib-defaults",
    vendor_available: true,
    defaults: ["llvm10-defaults"],
    export_header_lib_headers: ["llvm10-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
