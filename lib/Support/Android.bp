cc_library_static {
    name: "libLLVM50Support",
    vendor_available: true,
    defaults: ["llvm50-defaults-no-generated-headers"],
    target: {
        // llvm50-defaults turns off host builds by default, turn it back on for
        // modules that are used to build llvm50-tblgen
        host: {
            enabled: true,
        },
        windows: {
            enabled: true,
        },
    },
    srcs: [
        "*.c",
        "*.cpp",
    ],
}
