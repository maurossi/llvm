cc_library_static {
    name: "libLLVM60Support",
    vendor_available: true,
    defaults: ["llvm60-defaults-no-generated-headers"],
    target: {
        // llvm60-defaults turns off host builds by default, turn it back on for
        // modules that are used to build llvm60-tblgen
        host: {
            enabled: true,
        },
        windows: {
            enabled: true,
        },
    },
    srcs: [
        "*.c",
        "*.cpp",
    ],
}
